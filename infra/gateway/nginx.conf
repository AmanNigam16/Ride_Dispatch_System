events {}

http {
  server {
    listen 80;

    location /api/auth {
      proxy_pass http://host.docker.internal:5001;
    }

    location /api/rides {
      proxy_pass http://host.docker.internal:5002;
    }
  }
}